# KubeMemory Frontend — copy to .env.local. Never commit .env.local.
# All frontend env vars must be prefixed with VITE_

# Backend API (no trailing slash)
VITE_API_URL=http://localhost:8000/api

# WebSocket URL (no trailing slash; same origin or ws:// host)
VITE_WS_URL=ws://localhost:8000/ws
